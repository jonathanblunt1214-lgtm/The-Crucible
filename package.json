{
  "name": "the-crucible-nexus-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Optional Nexus plugin for fail-closed project-isolated scientific learning and canonical Crucible governance references.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/plugin.test.js",
    "verify": "node scripts/verify.js",
    "package": "node scripts/package.js"
  }
}
